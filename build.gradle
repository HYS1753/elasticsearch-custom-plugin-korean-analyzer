buildscript {
  repositories {
    mavenLocal()
    mavenCentral()
  }

  dependencies {
    classpath "org.elasticsearch.gradle:build-tools:8.12.2"
  }
}

apply plugin: 'elasticsearch.esplugin'
apply plugin: 'elasticsearch.yaml-rest-test'

esplugin {
  name 'korean-analyzer'
  description 'elasticsearch custom plugin - Korean analyzer (jamo, choseong, kor2eng, eng2kor) by hys1753@naver.com'
  classname 'elasticsearch.custom.plugin.KoreanAnalyzerPlugin'
}

group = "elasticsearch.plugin"
version = "1.0"
sourceCompatibility = '17'


repositories {
  mavenCentral()
}


dependencies {
  implementation group: 'org.elasticsearch', name: 'elasticsearch', version: '8.12.2'
  implementation 'org.apache.commons:commons-lang3:3.16.0'
  yamlRestTestRuntimeOnly "org.apache.logging.log4j:log4j-core"

  testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api', version: '5.7.0'
  testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-engine', version: '5.7.0'

}

test {
  useJUnitPlatform()
}

